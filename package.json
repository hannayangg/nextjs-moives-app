{
  "name": "movies-api",
  "version": "1.0.0",
  "description": "JSON Server API for movies",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 4000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}